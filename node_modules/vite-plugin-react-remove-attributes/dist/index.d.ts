import { Plugin } from 'vite';
import { FilterPattern } from '@rollup/pluginutils';

interface Options {
    attributes: string[];
    include?: FilterPattern;
    exclude?: FilterPattern;
}

declare function VitePluginReactRemoveAttributes({ include, exclude, attributes, }: Options): Plugin;

export { VitePluginReactRemoveAttributes as default };
